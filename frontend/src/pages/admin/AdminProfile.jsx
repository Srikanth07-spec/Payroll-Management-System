import { useState } from "react";
import { Camera, ShieldCheck } from "lucide-react";
import { loadData, saveData } from "../shared/payrollData";
export default function AdminProfile({ currentUser }) { const [pic,setPic]=useState(""); const save=(e)=>{const file=e.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{setPic(reader.result);const profiles=loadData("payroll_admin_profile",{});saveData("payroll_admin_profile",{...profiles,profilePic:reader.result});};reader.readAsDataURL(file)}; return <><div className="section-title"><div><h2>Administrator Profile</h2><p>Only the profile picture can be changed by the administrator.</p></div></div><div className="profile-card-large admin-profile-only"><div className="profile-photo-large">{pic?<img src={pic} alt="Admin profile"/>:<ShieldCheck size={48}/>}<label><Camera size={17}/><input type="file" accept="image/*" onChange={save}/></label></div><div className="profile-large-info"><h2>{currentUser?.displayName||"Administrator"}</h2><span>Administrator</span><div className="profile-details"><div><span>Email</span><strong>{currentUser?.email}</strong></div><div><span>Account</span><strong>Firebase Administrator</strong></div></div></div></div></> }
