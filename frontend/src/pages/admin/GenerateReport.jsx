import { FileBarChart, UserRoundSearch, Activity } from "lucide-react";
export default function GenerateReport({ setPage }) { return <><div className="section-title"><div><h2>Generate Report</h2><p>Choose one of the two administrative reports.</p></div></div><div className="report-choice-grid"><button className="report-choice employee-report" onClick={()=>setPage("employee-report")}><UserRoundSearch size={34}/><div><h3>Employee Report</h3><p>Employee information, daily work activity, login and logout history, leaves and documents.</p></div><span>Open Report →</span></button><button className="report-choice attendance-report" onClick={()=>setPage("attendance-report")}><Activity size={34}/><div><h3>Attendance Report</h3><p>Generate attendance records for a selected employee and open related documents and leave history.</p></div><span>Open Report →</span></button></div></> }
